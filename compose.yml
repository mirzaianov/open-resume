services:
  open-resume:
    build: .
    container_name: open-resume
    ports:
      - "127.0.0.1:${PORT}:3000"
    restart: unless-stopped
    networks:
      - dashboard_net

networks:
  dashboard_net:
    external: true
